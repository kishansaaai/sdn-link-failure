"""The supported API is registered by SDNController at API_PORT (default 5000).

Endpoints: /health, /topology, /paths, /recovery-log, /metrics.
The original POX/Flask adapter remains in legacy/monitor_api.py.
"""
